name: Security and quality

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  tests:
    name: Tests on Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.13"]
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - uses: actions/setup-python@e797f83bcb11b83ae66e0230d6156d7c80228e7c # v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: |
            requirements-dev.txt
            docs/server/requirements.lock
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.txt
          python -m pip check
      - name: Compile Python
        run: python -m compileall -q docs/server tests scripts
      - name: Validate JavaScript syntax
        run: node --check docs/assets/app.js
      - name: Run unit, integration, and regression tests
        env:
          KELLMARKS_DATA_FILE: ${{ runner.temp }}/kellmarks-ci-data-${{ matrix.python-version }}.json
        run: python -m pytest

  quality-security-build:
    name: Quality, security, dependencies, and clean build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - uses: actions/setup-python@e797f83bcb11b83ae66e0230d6156d7c80228e7c # v6
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: |
            requirements-dev.txt
            docs/server/requirements.lock
      - name: Install fresh environment
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.txt
          python -m pip check
      - name: Static analysis and linting
        run: |
          ruff check .
          mypy
      - name: SAST and secret pattern scan
        run: |
          bandit -r docs/server -ll -x docs/server/instance
          python scripts/scan_secrets.py
      - name: Dependency vulnerability audit
        run: pip-audit --no-deps -r docs/server/requirements.lock
      - name: Release and configuration validation
        run: python scripts/validate_release.py
      - name: Performance regression check
        env:
          KELLMARKS_DATA_FILE: ${{ runner.temp }}/kellmarks-benchmark-data.json
        run: python scripts/benchmark.py
      - name: Server smoke test
        env:
          KELLMARKS_DATA_FILE: ${{ runner.temp }}/kellmarks-smoke-data.json
        run: |
          python docs/server/app.py >"${RUNNER_TEMP}/kellmarks-server.log" 2>&1 &
          server_pid=$!
          trap 'kill "$server_pid" 2>/dev/null || true' EXIT
          for attempt in {1..20}; do
            if curl --fail --silent http://127.0.0.1:8787/api/health >"${RUNNER_TEMP}/health.json"; then
              break
            fi
            sleep 0.5
          done
          python - <<'PY'
          import json
          import os
          from pathlib import Path

          health = json.loads((Path(os.environ["RUNNER_TEMP"]) / "health.json").read_text())
          assert health["ok"] is True
          assert health["version"] == "02.00.00"
          assert health["dataSchemaVersion"] == 2
          PY
